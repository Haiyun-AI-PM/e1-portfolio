# E1 拿来说明（作品集初始页是怎么借助 AI 做出来的）

> 选 3 个关键决策，每个讲清：当时的问题 → 给 AI 的输入 → AI 的产出 → 我的判断与修改 → 结果对比。

## 决策 1：动手前先查平台要求，不凭印象开工

- **当时的问题**：不确定 E1 到底要交什么、在哪一步做、按什么标准评分。凭印象直接生成一个网页很可能做偏，甚至踩"一句话指令直接提交"的红线。
- **给 AI 的输入**："帮我查下我的 EduSeed 上有哪几个挑战" → "帮我查下 E1"（要求先列出挑战、再展开 E1 的交付物与评分维度）。
- **AI 的产出**：6 个挑战列表（E1–E6）；E1 详情：交付 6 项（三件套 + E1_AI日志 + E1_拿来说明 + E1_AAR）、满分 100 的评分维度（envOutput 20 / gitQuality 20 / aiUsage 25 / adaptation 15 / reflection 20）、截止 2026-12-31、关卡规定的"先检查工作空间 → 询问 → 确认后才生成"流程。
- **我的判断与修改**：按关卡规定的行为流程走，先做授权/连接检查，再建独立项目目录 `E1-portfolio/`。
- **结果对比**：没有做偏方向；后续每一步都有据可依（比如"先询问再生成"就是从 brief 里来的）。如果跳过这步直接生成，可能漏掉 AI 日志/拿来说明/AAR 这类文件型交付物。

## 决策 2：生成页面采用"结构 / 样式 / 行为"三件套分离

- **当时的问题**：初始页要能预览、能运行，而且要能支撑 E2–E6 一路迭代成真正的作品集，不能堆一个一次性死页面。
- **给 AI 的输入**：要求生成 `index.html` / `styles.css` / `script.js` 三个文件：HTML 只管内容结构；CSS 用 `:root` 设计变量统一颜色/字体/间距；JS 只管行为（如页脚年份自动更新），并注释说明各自职责。
- **AI 的产出**：三件套共 264 行，职责分离、带注释、CSS 变量可全局换肤、预留了后续项目展示的结构。
- **我的判断与修改**：不直接相信"能跑"，先 `node --check script.js` 做语法检查，再核对 HTML 中引用的 `styles.css` / `script.js` 与磁盘文件一致，最后本地预览确认渲染正常。
- **结果对比**：页面可运行、可预览；后续要加作品卡片、改主题色，只需在既有结构上增量修改，不用推倒重来。

## 决策 3：首次提交前先配置 Git 身份，用规范提交信息入库

- **当时的问题**：`git config user.name / user.email` 为空。直接 `git commit` 会留下"无主提交"，且 E5 推送 GitHub 时无法归属到个人。
- **给 AI 的输入**：建仓前先检查本机 Git 身份；为空则停下，向用户询问提交者姓名与邮箱，确认后才提交。
- **AI 的产出**：用户提供身份（Lhy · 184377817@qq.com）；以**仓库级**配置写入（不动全局配置）。
- **我的判断与修改**：用规范提交信息 `feat: 初始化个人作品集初始页`；提交后核对 `git log` 与 `git status`（工作树干净）确认入库成功。
- **结果对比**：提交 697bc10 归属正确、信息清晰、仓库状态干净；E5 推送 GitHub 时同一身份可正确关联提交记录。
