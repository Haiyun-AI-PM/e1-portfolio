# E1 AAR 复盘

> AAR = After Action Review（KSTAR 中 K → K′ 的知识更新环节）。2026-09-04 开工当天完成复盘，**已由本人审阅补充并定稿**（本人补充：最想记住的点、R̂ 预期 70 分；第 5/6 维内容经本人确认与真实经历一致）。

## 1. 我学到了什么

- E1 是"经验作品集"聚合课程的第一关，目标不是做出多炫的页面，而是**跑通一条可复用的生产链路**：查 brief → 确认范围 → 生成 → 验证 → 建仓提交 → 过程材料。
- 网页三件套的职责分离：`index.html` 管结构、`styles.css` 管长相（颜色/字体/布局）、`script.js` 管行为；CSS 用 `:root` 设计变量后，换主题色只改一处。
- Git 首次提交的前置条件：`user.name` / `user.email` 为空时不能直接 commit，否则留下无法归属的"无主提交"，后续推 GitHub 也无法关联到个人；提交信息要有规范前缀（如 `feat:`）。
- 平台按"文件名关键词"校验交付物（文件名要含 `AI日志` / `拿来说明` / `AAR`），先过 `check-deliverables` 再提交，别硬交。
- **（本人）这次最想记住的 1 个点：提交前要查 Git 身份**。提交是永久写历史，身份为空会留下无法归属的"无主提交"，后续推 GitHub 也无法关联到自己——所以身份这类前置条件要在提交前主动自查，而不是等报错。

## 2. 我的流程

| 阶段 | 时间投入 | 做了什么 | 卡点 |
|---|---|---|---|
| 查要求 | ~5 分钟 | 列出 6 个挑战（E1–E6）→ 展开 E1 详情（交付物/评分维度/截止） | 口语"E1"与平台正式编号（ch-2026…长串）对不上，要先映射 |
| 环境准备 | ~5 分钟 | 授权/连接检查 → 建独立项目目录 `E1-portfolio/` | 无 |
| 生成初始页 | ~10 分钟 | 经确认后生成三件套 → node 语法检查 → 资源引用核对 → 本地预览 | 无（先确认再生成，符合关卡流程） |
| 首次提交 | ~10 分钟 | 查 Git 身份 → 发现为空 → 询问用户 → 仓库级配置 → commit `feat: 初始化个人作品集初始页`（697bc10） | **Git 提交身份缺失，需停下询问** |
| 过程材料 | 进行中 | AI 日志 / 拿来说明 / AAR 起草 | 感受类维度需本人口述补充 |

## 3. 我与 AI 的协作

| 协作动作 | 具体例子 | 效果 | 反思 |
|---|---|---|---|
| 先查 brief 再动手 ✅ | "帮我查下 E1" → 拿到交付物 6 项与评分维度 | 全程有据可依，没做偏 | 好指令 = 带明确对象（查什么、为什么查） |
| 生成前确认范围 ✅ | 关卡流程要求先问"要生成初始页吗？"确认后才生成 | 避免未授权产出与"一句话提交"红线 | 符合平台流程本身也是一种"拿来说明"素材 |
| 每步验证再前进 ✅ | 语法检查 → 引用核对 → 预览 → 才进 Git | 生成物可运行才入库 | 别信"应该能跑"，跑过才算 |
| 用简称直接查挑战 ❌ | 直接按"E1"查，平台正式编号是 ch-20260903033602-lcxfee | 一次查询失败，需重新映射 | 平台对象用平台返回的正式 ID，简称只在展示层用 |
| 迭代案例 🔄 | 页面生成后未直接提交，先补验证，再到提交前发现身份缺失、停下询问后配置 | 修正后一次提交成功（697bc10） | 前置条件（身份/编号）应在更早阶段自查 |

## 4. 我完成了什么

对照 E1 验收要点：

| 验收点 | 状态 | 说明 |
|---|---|---|
| 页面三件套存在、可运行 | ✅ | `index.html` / `styles.css` / `script.js`（264 行），node 语法检查通过，预览正常 |
| Git 建仓 + 首次规范提交 | ✅ | `main` 分支根提交 697bc10，提交信息 `feat: 初始化个人作品集初始页`，工作树干净 |
| AI 使用有迭代记录 | ✅ | 多轮指令 + 本 AI 日志佐证（查→确认→生成→验证→提交） |
| 拿来主义说明 | ✅ | E1_拿来说明.md 选 3 个关键决策，来源/改动/理由清楚 |
| 复盘 | ✅ | AAR 已由本人审阅补充并定稿（2026-09-04） |

自我评价（已由本人确认）：E1 的核心链路已完整跑通，交付物齐全、提交规范；三份过程材料基于本次真实会话记录起草并经本人审阅，无虚构内容。

## 5. 卡点与突破

- **最大卡点**：Git 提交身份缺失。首次提交前检查 `git config` 发现 `user.name` / `user.email` 为空——直接 commit 会永久写入一条无法归属的历史记录。
- **解决方式**：不猜、不随便填，停下向用户询问真实姓名与邮箱（Lhy · 184377817@qq.com），以仓库级配置写入后再提交。
- **"真正进脑子"的瞬间**：意识到"提交是**永久写历史**，且 E5 推送 GitHub 要靠同一身份归属"——所以这类前置条件应该在建仓时就查清，而不是到提交前才暴露。
- （本人确认）第 5 维以上描述即本次最卡环节，与我的实际经历一致，无需改动。

## 6. 改进方向（下次同类挑战怎么做不同）

- E2 起开工第一步就自查两件事：Git 身份是否已配置、平台挑战编号/别名能否直接查询，把前置条件前置。
- 每关先 `get-challenge` 拿权威 brief，把交付物清单和评分维度贴出来对表，做完一项划一项。
- 提交前先跑本地 `check-deliverables` 预检，缺件先补再谈提交。
- 过程材料边做边记（AI 日志每天记），不要最后一口气补——补出来的容易失真。
- （本人承诺）以后每次提交前先自查 `git config user.name / user.email`：为空就停下问清楚，绝不带着空身份 commit——把 E1 这条教训变成固定动作。

## 7. KSTAR 学习信号

- **R̂（预期）**：**70 分**（本人预估，定稿时给出）。说明：初稿客观自评曾写 ≥ 90 分（交付物齐全、Git 规范、AI 协作多轮有据），本人最终预估 70 分——差异本身即校准点，留待评分回来后做 ΔR 归因。
- **R（实际）**：⏳ 待提交后回填（AI 初评约 1–5 分钟，飞书通知；教师终审另计）。
- **ΔR 归因**：⏳ 评分返回后回填（K 知识 / S 技能 / T 任务理解 / Â 行动 哪个环节造成偏差）。
- **K′（知识更新，下次带入）**：先查 brief → 确认范围再生成 → 建仓前查身份 → 材料齐了、预检过了才提交；平台对象用正式 ID。
