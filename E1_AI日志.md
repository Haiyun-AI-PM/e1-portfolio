# E1 AI 日志（2026-09-04，开工当天）

> 本日志只记录当天与 AI 协作的**事实**：用了什么工具、关键指令、输出哪里对/哪里错、怎么修正。

## 用到的工具/环境

| 工具 | 用途 |
|---|---|
| CogSeed（Commander 编排） | 主协作环境，统一指挥生成、检查、提交 |
| EduSeed 课程客户端插件（runtime 命令） | `license-check` / `health` 连接检查、`list-challenges` / `get-challenge` 查挑战与评分要求 |
| git 2.50.1 | 仓库初始化与首次提交 |
| node（语法检查）+ 本地预览 | 生成后的可运行性验证 |

## 关键过程（事实流水）

1. **查挑战**：先 `list-challenges` 确认课程共 6 关（E1–E6），再 `get-challenge` 取 E1 详情（交付物 6 项、评分维度、截止 2026-12-31）。
2. **环境检查**：开工前先跑授权/连接检查，确认席位与平台连通正常，避免做一半才发现没权限。
3. **建项目目录**：在 `E1-portfolio/` 下开工（与之前"查询挑战"的工作区分开，不污染）。
4. **生成三件套**：按 E1 关卡流程，先问"要先生成一个可以预览的作品集初始页吗？"经确认后才生成 `index.html` / `styles.css` / `script.js`。
5. **验证**：`node --check script.js` 语法通过；检查 HTML 里引用的样式/脚本文件名与磁盘一致；本地预览成功。
6. **首次 Git 提交**：建仓前先查 `git config user.name/email`，发现为空 → 停下询问用户 → 配置为仓库级身份（Lhy · 184377817@qq.com）→ `git add` → `git commit -m "feat: 初始化个人作品集初始页"`（697bc10）→ 工作树干净。

## 输出哪里对 / 哪里错 / 怎么修正

- **对（值得保留）**：生成页面后没有直接提交，而是先做语法检查 → 引用一致性检查 → 预览，验证通过才进 Git 流程，符合"每步验证、小步走"的节奏。
- **对（值得保留）**：提交前先检查 Git 身份；身份为空时停下询问，而不是随便填一个名字——首次提交会永久写入历史。
- **错/坑 1**：平台正式挑战编号是长串 `ch-20260903033602-lcxfee`，用口语"E1"直接查会查不到，需要先映射简称 → 修正：查询以平台返回的正式 challenge_id 为准。
- **错/坑 2**：默认以为本机 Git 已配置好身份，实际是空的 → 修正：开工建仓前先把身份这一前置条件查清，避免到提交那一步才卡住。

## 今天有效 / 无效做法总结

- **有效**：先查平台 brief 再动手；生成前确认范围；每步验证（语法→引用→预览→提交）；提交前解决身份等前置条件。
- **无效**：没有明显无效做法；唯一的低效点是 Git 身份问题到提交前才发现，下次应在建仓时就查。
